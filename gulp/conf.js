'use strict';

var watchify = require('watchify');
var path = require('path');

exports.paths = {
  src: 'src/front-end',
  components: 'src/front-end/components',
  dist: 'dist',
  publish: 'publish'
};
